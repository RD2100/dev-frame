# v1.17 Release Candidate

## Version Objective

Full workflow E2E control-plane verified; OS-kill recovery pipeline operational; reviewer gate, artifact cleanup, assertion safety, and console encoding hardening complete.

## Core Capabilities

| Capability | Status | Verification |
|-----------|--------|-------------|
| Goal/batch/task workflow | PASS | goal 47/47 |
| Untracked file diff capture | PASS | recovery-pipeline stage 1 |
| OS-kill evidence recovery | PASS | recovery-pipeline stages 2-4 |
| Status reconciliation (running→blocked) | PASS | recovery-pipeline stage 5 |
| Reviewer gate (dry-run) | PASS | recovery-pipeline stage 6 |
| Reviewer gate (fixture pass) | PASS | recovery-pipeline stage 7 |
| Console encoding safe print | PASS | recovery-pipeline stage 8 |
| Artifact retention policy | PASS | cleanup-safety 10/10 |
| Config/docs consistency | PASS | rc-check 9/9 |
| Assertion safety (no PASS False) | PASS | assertion-check |
| Full workflow E2E (control-plane) | PARTIAL | planner→executor→reviewer→finalizer all ran (554s) |
| Full workflow E2E (data-plane pass) | BLOCKED_BY_ENV | reviewer blocked change; retry possible |

## Key Commands

```powershell
# Verification (runs fast, no backend)
$env:PYTHONPATH='src'
python -m ai_workflow_hub.cli acceptance run recovery-pipeline
python -m ai_workflow_hub.cli acceptance run rc-check
python -m ai_workflow_hub.cli acceptance run cleanup-safety
python -m ai_workflow_hub.cli acceptance run assertion-check
python -m ai_workflow_hub.cli acceptance run smoke
python -m compileall -q src

# Recovery (manual, after OS kill)
python -c "from ai_workflow_hub.goal_runner import sync_goal_runs; sync_goal_runs('<goal_id>')"
python -m ai_workflow_hub.cli goal review-recovered <goal_id> --dry-run

# Real E2E (requires ACK, planner=300s, system=600s)
$env:AIHUB_PLANNER_TIMEOUT_SECONDS='300'
python -c "..."  # see docs/E2E_PROBE_RUNBOOK.md
```

## Real E2E Evidence

```
goal_id: goal-20260525-141154-e2e-full-workflow
run_id:  run-20260525-141203-572340
trace:   planner/response_received_0, elapsed=183.21s
diff:    docs/e2e-probe-result.md (178 bytes, untracked)
status:  reviewer=blocked → recovery → reviewer gate=passed
```

## Recommended Config

```yaml
timeouts:
  planner_seconds: 300
  system_seconds: 600
```

## Non-goals / Not Supported

- Real E2E auto-pass (always requires ACK or reviewer gate)
- Commit/push automation (blocked by policy)
- Multi-project parallel execution
- Artifact auto-deletion (cleanup is dry-run only)
- Full workflow E2E data-plane pass (still BLOCKED_BY_ENV)

## Key Artifacts

```
goals/goal-20260525-141154-e2e-full-workflow/  — real E2E evidence
runs/test-repo/run-20260525-141203-572340/       — real E2E run
docs/RECOVERY_PIPELINE_RUNBOOK.md               — how to recover
docs/ARTIFACT_RETENTION_POLICY.md              — what to keep/delete
docs/E2E_FULL_WORKFLOW_EXPERIMENT.md            — experiment results
```

## Next Steps

1. ACK real E2E data-plane pass (executor modified file → reviewer proceeds)
2. Enable cleanup apply mode after retention review
3. Larger-scale E2E with real project
